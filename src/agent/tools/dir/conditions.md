# Conditions

Conditions are boolean expressions evaluated at runtime.

Examples:
- age >= 18
- accountStatus == "ACTIVE"
- income > threshold

Conditions should:
- Be mutually exclusive when possible
- Avoid overlapping logic
- Be ordered from most specific to least specific
